#include <iostream>

int find_sum(int x, int y) {
    std::cout << " txt from find_sum " << std::endl;
    return x + y;
}

int main() {
    std::cout << " txt from main " << std::endl;
    return 0;
}